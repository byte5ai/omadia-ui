import DefaultTheme from 'vitepress/theme';
import './lume-tokens.css'; // GENERATED from the spec by scripts/generate.mjs (prebuild)
import './lume.css'; // maps spec tokens → VitePress vars + Lume material

// The site embodies the design system it documents: theme colors, fonts, radii
// are derived from docs/visual-spec.md §2; the Lume recipes (§3) live in lume.css.
export default DefaultTheme;
